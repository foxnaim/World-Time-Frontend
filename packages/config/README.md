# @worktime/config

Shared config for apps and packages in the WorkTime monorepo.

Provides TypeScript presets (base, nextjs, nestjs, library) under `tsconfig/`
and flat-config ESLint presets (base, react) under `eslint/`.
